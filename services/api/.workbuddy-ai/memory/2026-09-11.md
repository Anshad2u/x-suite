# 2026-09-11

## Twitter/X project audit (cross-folder search)
Searched `C:\Users\muzai` (home), `VS Code Projects`, Desktop, Documents, Downloads for related Twitter/X work.

Three related projects, all active Aug 2026:

1. **follower-dashboard** (this project) — `C:\Users\muzai\VS Code Projects\follower-dashboard`
   Flask + Neon Postgres + Scweet X scraper + autopost engine. Also Reddit (praw) + Telegram approval. Vite `dashboard/` frontend. Last commit 2026-08-22; files touched to Aug 30.
2. **x-growth-hub** — `C:\Users\muzai\x-growth-hub`
   Next.js 16 (next-forge fork) frontend; README states its Flask backend is `../follower-dashboard`. Adds "XActions" suite: content calendar, best-time heatmap, pattern recognition, tweet cloner, growth scraper + scoring. Commits 2026-08-20/21.
3. **social-agent** — `C:\Users\muzai\social-agent`
   Standalone Python agent for X + Reddit *maintenance*: scheduled posts, mention watcher, Telegram alerts, X free-tier budget hard-stop at 480/500. Uses official X API v2 (OAuth 1.0a), no scraping. Modified 2026-08-28.

Key relationship: x-growth-hub is the frontend for follower-dashboard's Flask API; social-agent is an independent official-API tool (different approach from the scraper-based follower-dashboard).

Non-matches checked: backlink-pilot (backlink submissions), OpenAgentsControl / schedulechat / next-forge-saas-starter (only incidental "x.com" in badges / "svix.com").

## Merge plan for the 3 X projects
Wrote `MERGE_PLAN.md` at the follower-dashboard root. User's locked decisions:
- X access: **scraper only (Scweet)** — drops social-agent's OAuth1 client
- Frontend: **Next.js only** — retire Vue index.html + Vite dashboard/ (after porting Scrape/Groups/Followers screens)
- Layout: **monorepo** — apps/web (Next.js), services/api (Flask), packages/agent (social-agent)
- DB: **Postgres only** — SQLite retired

Key analysis: social-agent's `posts`/`mentions`/`counters` map to a new `post_queue` table + existing `mentions`/`reddit_mentions` + a new `counters` table. Its `x_client.py`, `reddit_client.py`, `watcher.py` are duplicates and get deleted; `drafts.py` (markdown queue) is unique value and is kept. Dropping the official API removes the 480/500 monthly cap — daily cap + jitter become the only throttle. Two post tables (`post_queue` for pending work, `posted_log` for provenance/learning) are linked in one transaction.

Open item: build an `XBackend` seam in Phase 2 to keep the option of reintroducing the official API later.
